package LinkedList;
import java.util.*;

// class Node {
//     int data;
//     Node next, pre;

//     Node(int data) {
//         this.data = data;
//     }
// }
// // create nde;
// // taile kai next ko p kai previous sai connect karenge;
// // tail = p;
// // p ko tail sai connect karenge;
// // taile == null;

// class DoublyLinkList {
//     Node head, tail;
//     int leng;

//     public void addLast(data) {
//         if(head==null) {
//         head = tail = new Node(data); 
//         return;
//         }

//         Node p = new Node(date);
//         p.pre = tail;
//         tail.next = p;
//         tail = p;

//     }

    
    
    
// }

class Node {
    int data;
    Node next, pre;

    Node(int data) {
        data = this.data;
    }
}

class doubli {
    Node head, tail;
    int length;

    public addFirst(int data){
        if(head == null){
            head = tail = new Node(data);
            return;
        }
        else{
            Node p = new Node(data);
            p.tail
        }
    }
}
